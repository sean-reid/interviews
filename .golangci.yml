version: "2"

linters:
  settings:
    errcheck:
      # CLI output to injected writers; a failed write to stdout has no
      # useful recovery here.
      exclude-functions:
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln
